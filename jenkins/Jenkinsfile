pipeline {
  agent { label 'slave' }

  options {
    timestamps()
  }

  stages {
    stage('Checkout') {
      steps {
        sshagent(credentials: ['github']) {
          git branch: 'master', url: 'git@github.com:rozana10710/demo2-jenkins-ansbile-docker-webapp.git'
        }
        sh 'echo "Checked out commit:" && git log -1 --oneline || true'
      }
    }
  }
}
